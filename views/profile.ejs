<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/public/stylesheets/style.css" />
  </head>
  <body>
    <div class="container">
      <img src="<%= person.image %>" alt="" />
      <h2><%= person.name %></h2>
      <p><%= person.status %></p>
    </div>
    <div class="form-wrapper">
      <form
        class="update-status"
        action="/update_status"
        method="POST"
        enctype="multipart/form-data"
      >
        <legend>Update Status</legend>
        <div class="hidden">
          <label for="name">Name</label>
          <input required name="name" type="text" value="<%=person.name%>" />
        </div>
        <div>
          <label for="staus">Status</label>
          <input name="status" type="text" />
        </div>
        <div><input type="submit" value="Update Status" /></div>
      </form>
      <form
        class="update-image"
        action="/update_image"
        method="POST"
        enctype="multipart/form-data"
      >
        <legend>Update Profile Picture</legend>
        <div class="hidden">
          <label for="name">Name</label>
          <input required name="name" type="text" value="<%=person.name%>" />
        </div>
        <div>
          <label for="image">Profile Picture</label>
          <br />
          <input type="file" name="image" />
        </div>
        <div><input type="submit" value="Update Profile Picture" /></div>
      </form>
    </div>
  </body>
</html>
